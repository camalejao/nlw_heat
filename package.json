{
    "name": "nlw_heat",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "serve": "cd node && yarn prod",
        "heroku-postbuild": "cd web && yarn --prod=false && yarn build && cd ../node && yarn --prod=false && yarn build"
    }
}